let allNews=[];
const NEWS_URL='https://raw.githubusercontent.com/filipemelo1994-del/Mundo-em-foco-24-m-dia-/main/data/news.json';
const AUTO_URL='https://raw.githubusercontent.com/filipemelo1994-del/Mundo-em-foco-24-m-dia-/main/data/news-auto.json';
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const img=n=>n.image?`<img src="${esc(n.image)}" alt="${esc(n.imageAlt||n.title)}" loading="lazy">`:'';
const href=n=>`noticia.html?id=${encodeURIComponent(n.id)}`;
function render(list){const grid=document.getElementById('newsGrid');document.getElementById('newsCount').textContent=`${list.length} matérias`;document.getElementById('emptyState').hidden=!!list.length;grid.innerHTML=list.map(n=>`<article class="card"><a href="${href(n)}"><div class="thumb">${img(n)}</div><span class="label">${esc(n.category)}</span><h3>${esc(n.title)}</h3><p class="summary">${esc(n.summary)}</p><span class="meta">${esc(n.location)} • ${esc(n.date)}</span></a></article>`).join('')}
function newsTime(n){const m=String(n.date||'').toUpperCase().match(/(\d{1,2})\s+([A-ZÇ]+)\s+(\d{4})/);if(!m)return 0;const mon={JAN:0,FEV:1,MAR:2,ABR:3,MAI:4,JUN:5,JUL:6,AGO:7,SET:8,OUT:9,NOV:10,DEZ:11};return new Date(Number(m[3]),mon[m[2]]??0,Number(m[1])).getTime()}function boot(news){allNews=news.filter(n=>n.published!==false).sort((a,b)=>newsTime(b)-newsTime(a));if(!allNews.length)return;const h=allNews[0];document.getElementById('breakingText').textContent=h.title;document.getElementById('hero').innerHTML=`<a href="${href(h)}"><div class="photo">${img(h)}</div><div class="overlay"><span class="label">${esc(h.category)}</span><h1>${esc(h.title)}</h1><p>${esc(h.summary)}</p><span class="read">LER MATÉRIA COMPLETA →</span></div></a>`;document.getElementById('sideCards').innerHTML=allNews.slice(1,4).map(n=>`<article class="mini">${img(n)}<a href="${href(n)}"><span class="label">${esc(n.category)}</span><h3>${esc(n.title)}</h3></a></article>`).join('');render(allNews);document.getElementById('mostRead').innerHTML=allNews.slice(0,5).map((n,i)=>`<li><b>${i+1}</b><a href="${href(n)}">${esc(n.title)}</a></li>`).join('');document.getElementById('highlightGrid').innerHTML=allNews.slice(0,4).map(n=>`<article class="highlightCard">${img(n)}<a href="${href(n)}"><span class="label">${esc(n.category)}</span><h3>${esc(n.title)}</h3></a></article>`).join('')}
Promise.all([fetch(AUTO_URL+'?v='+Date.now(),{cache:'no-store'}).then(r=>r.ok?r.json():[]).catch(()=>[]),fetch(NEWS_URL+'?v='+Date.now(),{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('news');return r.json()})]).then(([auto,base])=>boot([...auto,...base])).catch(()=>{document.getElementById('breakingText').textContent='Mundo em Foco 24 • Informação que importa';document.getElementById('emptyState').hidden=false});
document.getElementById('menuBtn').onclick=()=>document.getElementById('nav').classList.toggle('open');document.getElementById('today').textContent=' • '+new Date().toLocaleDateString('pt-BR',{day:'2-digit',month:'2-digit',year:'numeric'});document.getElementById('searchInput').addEventListener('input',e=>{const q=e.target.value.toLowerCase().trim();render(!q?allNews:allNews.filter(n=>`${n.title} ${n.summary} ${n.category} ${n.location}`.toLowerCase().includes(q))) });